# Bárbara - Configurações de Ambiente
# Copie este arquivo para .env e ajuste os valores

# === API ===
NODE_ENV=development
PORT=3000
ALLOWED_ORIGINS=http://localhost:8080,http://localhost:3000

# === MongoDB ===
MONGODB_URI=mongodb+srv://nicolasrosaab_db_user:Gio4EAQhbEdQMISl@cluster0.npuhras.mongodb.net/

# === Avatar Provider (mock, ready-player-me, tryon-diffusion) ===
AVATAR_PROVIDER=mock

# Ready Player Me (usado quando AVATAR_PROVIDER=ready-player-me)
READY_PLAYER_ME_APP_ID=
READY_PLAYER_ME_API_KEY=
READY_PLAYER_ME_BASE_URL=https://api.readyplayer.me/v2/avatars

# TryOn Diffusion / Hugging Face (usado quando AVATAR_PROVIDER=tryon-diffusion)
TRYON_DIFFUSION_ENDPOINT=
TRYON_DIFFUSION_TOKEN=

# Storage base para arquivos gerados
ASSETS_BASE_URL=https://storage.example.com/avatars

# === Ngrok (para exposição pública) ===
NGROK_TOKEN=seu_token_ngrok_aqui

# === OpenAI ===
OPENAI_API_KEY=sk-...

# === HuggingFace ===
HF_TOKEN=hf_...

# === Azure OpenAI (Opcional) ===
AZURE_OPENAI_ENDPOINT=https://seu-resource.openai.azure.com
AZURE_OPENAI_API_KEY=sua_chave_aqui
AZURE_OPENAI_MODEL=gpt-4

# === Azure (Opcional) ===
AZURE_TENANT_ID=
AZURE_CLIENT_ID=
AZURE_CLIENT_SECRET=
AZURE_SUBSCRIPTION_ID=

# === Azure Container Registry (Opcional) ===
ACR_USERNAME=
ACR_PASSWORD=

# === GitHub (Opcional) ===
GITHUB_TOKEN=

# Observability (opcional)
SENTRY_DSN=
SENTRY_ENVIRONMENT=production
LOG_LEVEL=info

# === Fila Externa (Opcional) ===
# Opções: local, rabbitmq, azure-queue
AVATAR_QUEUE_MODE=local

# RabbitMQ (quando AVATAR_QUEUE_MODE=rabbitmq)
RABBITMQ_URL=amqp://localhost
AVATAR_QUEUE_NAME=barbara-avatar-jobs

# Azure Queue Storage (quando AVATAR_QUEUE_MODE=azure-queue)
AZURE_STORAGE_CONNECTION_STRING=
AZURE_QUEUE_NAME=barbara-avatar-jobs
AZURE_QUEUE_POLL_MS=5000
